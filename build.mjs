import { build } from "esbuild";import { mkdir,copyFile,rm } from "node:fs/promises";await rm("dist",{recursive:true,force:true});await mkdir("dist",{recursive:true});await Promise.all([copyFile("index.html","dist/index.html"),copyFile("styles.css","dist/styles.css"),copyFile("_redirects","dist/_redirects")]);await build({entryPoints:["app.js"],bundle:true,minify:true,outfile:"dist/app.js",format:"iife",target:"es2020"});
